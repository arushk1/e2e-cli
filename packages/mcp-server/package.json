{
  "name": "@e2e-cloud/mcp-server",
  "version": "0.1.0",
  "description": "MCP server for E2E Networks cloud",
  "bin": {
    "e2e-mcp": "./bin/e2e-mcp"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@e2e-cloud/sdk": "*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
